services:
  - type: web
    name: evalhvan-backend
    env: python
    region: oregon
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn EVALHVAN.wsgi:application --bind 0.0.0.0:$PORT
